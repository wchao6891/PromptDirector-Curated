name: 精选投稿预检

on:
  issues:
    types: [opened, edited, reopened, labeled]

permissions:
  contents: read
  issues: write

jobs:
  preflight:
    if: startsWith(github.event.issue.title, '[投稿]') || contains(github.event.issue.labels.*.name, 'submission')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 保存投稿内容
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          printf '%s' "$ISSUE_BODY" > "$RUNNER_TEMP/issue-body.md"

      - name: 预检投稿包
        id: preflight
        continue-on-error: true
        run: >-
          node tools/submission-preflight.mjs
          --issue-body "$RUNNER_TEMP/issue-body.md"
          --report "$RUNNER_TEMP/preflight-report.json"
          --payload "$RUNNER_TEMP/review-payload.zip"

      - name: 保存审核证据
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: submission-preflight-${{ github.event.issue.number }}
          path: |
            ${{ runner.temp }}/preflight-report.json
            ${{ runner.temp }}/review-payload.zip
          if-no-files-found: warn
          retention-days: 30

      - name: 更新预检状态
        if: always()
        uses: actions/github-script@v7
        env:
          PREFLIGHT_OK: ${{ steps.preflight.outcome == 'success' }}
          REPORT_PATH: ${{ runner.temp }}/preflight-report.json
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- promptdirector-submission-preflight -->';
            let report = { ok: false, message: '预检没有生成结果，请稍后重试。' };
            if (fs.existsSync(process.env.REPORT_PATH)) {
              report = JSON.parse(fs.readFileSync(process.env.REPORT_PATH, 'utf8'));
            }
            const passed = process.env.PREFLIGHT_OK === 'true' && report.ok === true;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const issue_number = context.issue.number;
            for (const name of ['preflight-passed', 'needs-fix']) {
              try {
                await github.rest.issues.getLabel({ owner, repo, name });
              } catch (error) {
                if (error.status !== 404) throw error;
                await github.rest.issues.createLabel({
                  owner, repo, name,
                  color: name === 'preflight-passed' ? '2da44e' : 'd73a4a'
                });
              }
            }
            const add = passed ? 'preflight-passed' : 'needs-fix';
            const remove = passed ? 'needs-fix' : 'preflight-passed';
            await github.rest.issues.addLabels({ owner, repo, issue_number, labels: [add] });
            try {
              await github.rest.issues.removeLabel({ owner, repo, issue_number, name: remove });
            } catch (error) {
              if (error.status !== 404) throw error;
            }
            const body = passed
              ? `${marker}\n预检通过：${report.caseCount} 个案例、${report.mediaCount} 个媒体。仍需人工审核后才会发布。`
              : `${marker}\n预检未通过：${report.message || '投稿包无法安全读取'}\n\n请在 PromptDirector 重新生成全部投稿文件后替换附件。`;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner, repo, issue_number, per_page: 100
            });
            const previous = comments.find((comment) => comment.user?.type === 'Bot' && comment.body?.includes(marker));
            if (previous) {
              await github.rest.issues.updateComment({ owner, repo, comment_id: previous.id, body });
            } else {
              await github.rest.issues.createComment({ owner, repo, issue_number, body });
            }
